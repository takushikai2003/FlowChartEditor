export class Node{
    constructor(id, type, label, x, y) {
        this.id = id;
        this.type = type;
        this.label = label;
        this.x = x; // x-coordinate for rendering
        this.y = y; // y-coordinate for rendering
    }
}